<Page
    x:Class="Twin.Views.BrowserTabView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Twin.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <TabView
            x:Name="tabView"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            AllowDropTabs="True"
            CanDragTabs="True"
            AddTabButtonClick="OnAddTab"
            TabCloseRequested="OnCloseTab"
            TabDroppedOutside="OnTabDroppedOutside"
            TabDragStarting="tabView_TabDragStarting"
            TabDragCompleted="tabView_TabDragCompleted"
            TabStripDragOver="tabView_TabStripDragOver"
            TabStripDrop="OnTabStripDrop"
            TabItemsChanged="tabView_TabItemsChanged"
            >
        <!--<TabView.TabItemTemplate>
            <DataTemplate>
                <TabViewItem Header="New Tab" Background="{ThemeResource SystemColorWindowColor}">
                    <TabViewItem.IconSource>
                        <SymbolIconSource Symbol="DockLeft"/>
                    </TabViewItem.IconSource>
                </TabViewItem>
            </DataTemplate>
        </TabView.TabItemTemplate>-->
    </TabView>
</Page>
